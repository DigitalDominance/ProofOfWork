{
  "name": "proof-of-work",
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network kaspaTestnet"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.1.2",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "hardhat": "^2.12.7"
  }
}
