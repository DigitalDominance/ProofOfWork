# ProofOfWork

## Setup

1. Clone repo
2. Copy `.env.example` to `.env` and fill in your private key and RPC URL
3. Install dependencies:
   ```bash
   npm install
   ```
4. Compile & deploy:
   ```bash
   npm run compile
   npm run deploy
   ```
